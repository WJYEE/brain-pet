/** Mirrors package.json's "version" — kept as a plain constant (not a package.json import) so it's usable from client components without bundling config JSON. Bump alongside package.json when cutting a release. */
export const APP_VERSION = '0.1.0'
